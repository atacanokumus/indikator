rules_version = '2';

// Ses dosyaları yalnızca sunucu tarafından (servis hesabı) yazılır/okunur.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
